
// This file is no longer needed as the project has fully transitioned to Supabase.
// All Firebase related configurations and initializations have been removed.

// If you were previously using Firebase Functions, you would also remove their initialization here.
// For example:
// import { getFunctions, httpsCallable, connectFunctionsEmulator } from "firebase/functions";
// const functions = getFunctions(app); 
// etc.
// These would be replaced with Supabase Edge Functions or database functions as needed.

console.warn("Firebase initialization file (src/lib/firebase.ts) is an empty stub. Project should be using Supabase.");

export {}; // Export an empty object or nothing to satisfy module requirements if imported elsewhere accidentally.
